import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Mode extends Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

Mode.propTypes = {};

export default Mode;
